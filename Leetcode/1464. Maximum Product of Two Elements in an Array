class Solution {
public:
    int maxProduct(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int size=nums.size();
        int a=nums[size-1] - 1;
        int b=nums[size-2] - 1;
        return a*b;
    }
};
